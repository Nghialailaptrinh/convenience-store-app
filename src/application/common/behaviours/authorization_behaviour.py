"""Template extension point for authorization before handlers.

Not configured: the local demo does not authenticate users. Implement with
IdentityService and User before exposing protected operations outside the demo.
"""
